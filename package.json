{
  "name": "node-starter",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon --exec node server/index"
  },
  "dependencies": {
    "@angular/cli": "^16.2.4",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "node": "^21.1.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
